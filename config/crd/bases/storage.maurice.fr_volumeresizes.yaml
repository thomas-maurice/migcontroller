---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: volumeresizes.storage.maurice.fr
spec:
  group: storage.maurice.fr
  names:
    kind: VolumeResize
    listKind: VolumeResizeList
    plural: volumeresizes
    singular: volumeresize
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.statefulSetName
      name: StatefulSet
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.currentReplica
      name: Replica
      type: integer
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .status.backupConfigMapName
      name: Backup
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VolumeResize is the Schema for the volumeresizes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VolumeResize
            properties:
              statefulSetName:
                description: StatefulSetName is the name of the StatefulSet to migrate
                minLength: 1
                type: string
              volumes:
                description: Volumes specifies which volumes to resize and their target
                  sizes
                items:
                  description: VolumeResizeTarget specifies a volume to resize
                  properties:
                    name:
                      description: Name is the name of the volumeClaimTemplate in
                        the StatefulSet
                      minLength: 1
                      type: string
                    newSize:
                      anyOf:
                      - type: integer
                      - type: string
                      description: NewSize is the target size for the volume (must
                        be smaller than current size)
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    storageClass:
                      description: StorageClass is the storage class to use for the
                        new PVC. Defaults to the original PVC's storage class.
                      type: string
                  required:
                  - name
                  - newSize
                  type: object
                minItems: 1
                type: array
            required:
            - statefulSetName
            - volumes
            type: object
          status:
            description: status defines the observed state of VolumeResize
            properties:
              backupConfigMapName:
                description: BackupConfigMapName is the name of the ConfigMap containing
                  the StatefulSet backup
                type: string
              completionTime:
                description: CompletionTime is when the migration completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the VolumeResize
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentReplica:
                description: CurrentReplica is the replica index currently being processed
                format: int32
                type: integer
              currentVolume:
                description: CurrentVolume is the volume name currently being processed
                type: string
              message:
                description: Message provides additional details about the current
                  phase
                type: string
              phase:
                description: Phase is the current phase of the migration
                enum:
                - Pending
                - Validating
                - Syncing
                - Replacing
                - Completed
                - Failed
                type: string
              startTime:
                description: StartTime is when the migration started
                format: date-time
                type: string
              volumeStatuses:
                description: VolumeStatuses tracks the status of each volume being
                  migrated
                items:
                  description: VolumeStatus tracks the migration status for a specific
                    volume on a specific replica
                  properties:
                    message:
                      description: Message provides additional details about the current
                        phase
                      type: string
                    newPVCName:
                      description: NewPVCName is the name of the temporary new PVC
                      type: string
                    oldPVCName:
                      description: OldPVCName is the name of the original PVC
                      type: string
                    oldPVName:
                      description: OldPVName is the name of the original PV (retained
                        for rollback)
                      type: string
                    phase:
                      description: Phase is the current phase of this volume's migration
                      enum:
                      - Pending
                      - Syncing
                      - Synced
                      - Replacing
                      - Completed
                      - Failed
                      type: string
                    replica:
                      description: Replica is the replica index this status is for
                      format: int32
                      type: integer
                    volumeName:
                      description: VolumeName is the name of the volume being migrated
                      type: string
                  required:
                  - phase
                  - replica
                  - volumeName
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
